{% extends 'base.html.twig' %}

{% block title %}Edit Reservering{% endblock %}

{% block body %}
    <div class="rand margin">
    <h1>Reservering bewerken</h1>

    {{ include('reservering/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-sm btn-primary" href="{{ path('reservering_index') }}">back to list</a>

    {{ include('reservering/_delete_form.html.twig') }}
    </div>
{% endblock %}
