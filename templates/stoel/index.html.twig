{% extends 'base.html.twig' %}

{% block title %}Stoel index{% endblock %}

{% block body %}
    <h1>Stoel index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Max_personen</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for stoel in stoels %}
            <tr>
                <td>{{ stoel.id }}</td>
                <td>{{ stoel.maxPersonen }}</td>
                <td>
                    <a href="{{ path('stoel_show', {'id': stoel.id}) }}">show</a>
                    <a href="{{ path('stoel_edit', {'id': stoel.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('stoel_new') }}">Create new</a>
{% endblock %}
