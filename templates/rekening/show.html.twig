{% extends 'base.html.twig' %}

{% block title %}Rekening{% endblock %}

{% block body %}
    <h1>Rekening</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rekening.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('rekening_index') }}">back to list</a>

    <a href="{{ path('rekening_edit', {'id': rekening.id}) }}">edit</a>

    {{ include('rekening/_delete_form.html.twig') }}
{% endblock %}
