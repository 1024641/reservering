{% extends 'base.html.twig' %}

{% block title %}Edit Rekening{% endblock %}

{% block body %}
    <h1>Rekening bewerken van {{ rekening.reservering }}</h1>

    {{ include('rekening/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-sm btn-primary" href="{{ path('rekening_index') }}">Terug</a>

    {{ include('rekening/_delete_form.html.twig') }}
{% endblock %}
