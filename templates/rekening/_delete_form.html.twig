<form method="post" action="{{ path('rekening_delete', {'id': rekening.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rekening.id) }}">
    <button cclass="btn btn-sm btn-primary" >Delete</button>
</form>
